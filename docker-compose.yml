web:
  image: manicmonkey/youtrack
  volumes_from:
    - data
  ports:
    - "8080:8080"

data:
  image: manicmonkey/youtrack
  container_name: youtrack_data
  volumes:
    - /data
  entrypoint: /bin/bash
